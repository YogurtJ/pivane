# Pivane 变更记录

## 未发布 · 持久用量账本与官方价补算

- 用量按已验证的原生记录增量入账，事务保存去重指纹与日汇总；支持按周和自然月查看，重启和删除会话后保留已入账历史。
- 后台同步每 5 分钟执行；保存读取位置和完整前缀校验值，确认原内容未变后只解析新增记录。文件改写、截断或替换时重新核对，游标与账本同一事务提交，旧账本自动补建游标。
- 网页删除会话前校验当前修订已完整入账，失败时拒绝删除；不修改原生聊天正文。
- 零或缺失费用按精确模型 ID 匹配内置官方供应商目录，保留原始费用与价格依据，处理已知阶梯价；未知型号明确显示未确认价格。新建且未配置价格的自定义模型使用匹配的目录价。

## 未发布 · 结果回执实时显示修复

- 修复来源线程空闲时收到原生 custom message 后，正文必须刷新才能看到结果回执的问题；实时追加、按交付标识去重，并阻止较早历史快照覆盖新回执，保留草稿、附件与阅读位置。
- 创建回执的状态文案明确为“创建时状态”，与后续结果区分；不修改原生历史记录。

## 未发布 · 任务工具参数兼容修复

- 修复模型展开全部可选参数时，创建任务携带结果分页字段而被拒绝的问题。工具按 action 整理请求，空的可选模型设置按未指定处理；REST 创建接口仍拒绝未知字段，来源身份与重复请求校验不变。

## 未发布 · 任务查找与结果回执

- 内置 Agent 任务查询使用分批、可重建的元数据目录；不再因项目会话总量超过64MiB或2000个会话一律拒绝，未核实、损坏或变化中的记录不误判为任务不存在。
- 新任务每轮结束绑定原生回复并保存纯文本预览，来源线程可查看运行/等待状态、结果与已读标记；空闲的受管 worker 保存回执，不自动启动模型，不为通知打开关闭的线程。
- 回执与已读从原生记录恢复，重复完成事件和重开不重复交付；子线程继续执行产生新的结果轮次，旧任务不集中推送历史通知。Agent 可按任务/run 标识分页读取原始回复。

## 未发布 · 架构与命名整理

- 日常开发采用连续 Git 主线与独立私有维护资料入口，新增模块导航、验证矩阵和命名迁移说明；整理路线图及已收录 RC4 功能的状态。
- 语法检查、测试、发行包及受管 Pi 更新快照共用源码发现规则，覆盖功能子目录和 TypeScript 扩展，继续排除私有资料与用户数据。
- 设置和媒体 HTTP 适配从主网关提取，worker 活动、维护空闲及回收判断由统一生命周期投影维护；未处理队列与不确定操作阻止回收或维护。
- 新工作台标记、工具来源及配置默认名称采用 Pivane；兼容既有 pi5 标记、数据文件和媒体凭据。标准启动支持 PIVANE 配置别名，原生 Pi 身份变量保持不变。
- 历史任务卡片保留原始 cwd，点击时核对旧路径别名与当前项目，再按原生 ID 打开线程；兼容目录迁移，拒绝跨项目或迟到跳转。
- 增加离线项目会话迁移组件：保留 ID、时间、完整分支和正文，只迁移头部项目归属及范围内父会话路径，保留原始备份并验证原生语义。目录、服务与其他路径引用须由完整维护流程协调，不自动迁移用户实例。

## 1.0.0-rc.4 · 同版本修订 · Pi 0.86.1

本修订包含下列原未发布功能。Pi 全家族统一锁定 0.86.1，替换旧 RC4 归档与标签；旧包 SHA256 和同版本手动升级限制见[发布说明](docs/releases/1.0.0-rc.4.md)。RC3 及更早版本不变。

### 默认后台常驻

- 普通安装默认使用后台服务，前台运行改为试用/排障入口；安装 Agent 需交付终端关闭后可达、实际身份与管理方式。
- 新增显式常驻安装器：Linux systemd 用户服务、macOS LaunchAgent、Windows 登录计划任务，macOS/Windows 默认创建桌面网页入口。
- 固定 Node/PATH/工作目录，拒绝占用端口和已有安装，记录 HTTP 就绪状态；Linux 开机常驻需配置 linger，个人电脑登录任务不承诺注销/睡眠可达。新跨平台安装器尚无 Mac/Windows 实机验收。

### Pivane 应用更新

- 版本卡片新增更新 Pivane，按渠道选择正式发布包，以一次性确认票据绑定版本与 SHA256。
- 独立目录校验归档、安装锁定依赖和执行隔离探针后，复用停机备份、启动与失败回退；不覆盖会话或自动重放任务。
- 忙碌时拒绝执行；首次使用需加载新版启动器。当前信任官方 GitHub HTTPS 与校验和，不宣称独立签名或全平台验收。

### 精选安装状态与助手预设

- 精选新增 pi-subagents 和 pi-hermes-memory，卡片按原生资源清单显示已安装、未安装、文件未就绪或待核对，并提供全局/当前项目范围与刷新。失败、未信任项目和无法识别的手动技能不误报为已安装或未安装；迟到响应不能覆盖新范围。
- 扩展助手“办公文档”改为通用 Word/PDF 能力、来源、配置与兼容性检查后给出安装方案，提供对应英文；移除“制作 PPT”需求气泡，保留精选入口。

### 扩展中心与精选

- 桌面左侧导航增加“扩展”，手机从项目／线程抽屉顶部进入；空白会话增加小型技能探索区，手机底栏保持原有布局。
- 独立扩展界面提供精选、已安装扩展包和已安装技能，设置原入口仍可跳转；沿用原生资源、安装确认、范围与修订保护。
- 精选收录 PPT Master、pi-mcp-adapter、pi-web-access 和 pi-computer-use，展示用途、来源、依赖与示例，并支持搜索。配置入口将来源带入扩展助手，不自动安装或发送请求；安装与当前会话加载分别核对。

## RC4 首次发布记录 · Pi 0.86.0 适配

- 应用版本与 package-lock 已更新为 `1.0.0-rc.4`，Pi Coding Agent 及配套官方依赖统一锁定 `0.86.0`；RC3 及更早发布资产保持不变。
- 适配 Pi 0.86 原生 `role: system` transcript 消息，过滤网页/侧聊/公开 RPC 展示，并从当前 worker 保留有效系统提示词与工具检查点。
- 会话 HTML/JSONL 导出、导入校验和 compaction system checkpoint 兼容 Pi 0.86；导入仍创建独立 session identity，不覆盖原始会话。
- 完成 Pi 0.86 的标题、侧聊、compaction、会话迁移、模型目录和维护流程回归；RC4 最终包必须以绑定归档 SHA256 的 validation.json 为准。
- 发布说明：[1.0.0-rc.4](docs/releases/1.0.0-rc.4.md)。

## RC4 已收录的功能

以下项目原先在开发过程中分别记录为未发布，现已纳入 RC4 修订；准确范围与验收以 RC4 发布说明和归档附件为准。

### Pi 0.86 适配

- 工作区及配套官方 Pi 依赖统一锁定到 `0.86.0`；已发布的 RC3 资产保持原样，不回写历史包。
- 适配 Pi 0.86 的原生系统消息：提示词/工具检查点保留在 Pi transcript 中，但不再作为浏览器聊天气泡、侧聊历史或公开 RPC 事件显示。
- 主会话上下文快照从 Pi 0.86 的当前 system message 读取有效系统提示词；HTML 导出继续包含实际运行时提示词，避免因原生导出接口变化出现空提示词。
- 测试运行器清除环境中的 Provider 凭据，避免 Pi 0.86 的 Radius/OpenRouter 离线目录把开发机认证带入合成测试；模型目录仍按实际认证状态展示。
- 保留 Pi 0.86 的离线 Radius 模型目录、按模型 compaction 配额、缓存保活和诊断能力；Pivane 暂不新增网页入口，未授权的模型不会自动执行。

### Pi 更新提醒

- 默认在可见页面使用期间静默检查 Pi 正式版，同一实例共享 24 小时成功缓存、并发请求和持久提醒记录；失败静默退避，支持关闭自动检查。
- 有新版时设置入口显示圆点，空闲时仅提醒一次；支持 3 天后提醒和忽略此版本，下一版仍可提示。
- “查看并更新”接入现有维护确认与命令日志，确认前重新核对版本和空闲状态，不自动下载或安装；保留草稿、附件与运行任务。

### 工作区导航

- 品牌区域保留彩色 π；Pi Agent 改用对话图标，与多媒体实验室及底部工具统一尺寸和选中色。
- 桌面增加常驻的折叠／展开按钮，折叠后位于品牌图标下方，支持键盘、操作提示及浏览器偏好记忆；保留品牌图标快捷操作。
- 窄屏平板和手机沿用图标栏与底部导航，不显示无效的折叠控件。

### 选区提问与朗读

- 主对话和侧聊消息正文选中文字后显示轻量菜单，支持加入主对话、在侧聊中提问和朗读选区；桌面支持右键与键盘菜单，触屏保留系统选择和复制入口。
- 引用以可展开、可移除的卡片加入对应草稿，保留已有文字和附件，不自动发送；提交失败保留草稿，切换线程沿用本页草稿与侧聊保留规则。
- 发送后引用随原生文字消息保存并显示为引用卡片；选区朗读沿用当前默认语音配置和单项票据执行，只读取所选文字，不自动重试。

### 子 Agent 设置体验

- 子 Agent 设置改为默认配置卡片和可展开的角色卡片，中文界面显示角色中文名及用途，原始标识保留在详情中；版本与配置规则收进说明区。
- 模型选择支持名称/完整 ID/供应商搜索、供应商筛选及每批40项的结果，避免超长下拉列表；支持键盘关闭、焦点恢复和手机内部滚动。
- 增加未保存项数与撤销草稿，保留继承、不可用模型原值、冲突草稿和安装状态；保存成功但重新读取失败时不再错误提示已核对成功。


### 设置页样式统一

- 设置各子页面统一使用图标、主标题和副标题的页头布局，字号、间距及刷新按钮沿用 Packages 的视觉风格。
- 供应商与模型、Skills 的刷新入口移至标题右侧；自定义 Provider 保留独立主按钮样式。手机端操作按钮换行，避免挤压标题和说明。

### 侧聊工具与执行确认

- 新增 sideChatTools 能力：新侧聊可读取和检索当前文件，并提供原生编辑、写入和平台命令工具；不再默认只有无工具问答。旧客户端省略 toolMode 时保留 none 兼容路径。
- 默认围绕侧问题调查和讨论，不主动续做继承任务；明确交办修改后，通过侧聊中的原生确认授权本次回复的修改与命令，结束后恢复询问。拒绝、超时、停止、伪造或过期确认不得执行或重放。
- 展示独立工具参数/结果/失败、等待确认及本次可修改状态；后台确认属于原侧聊，不显示在当前其他线程。保留冻结背景、三段本页暂存、草稿追加及独立统计。
- 工具模式使用公开 SDK 与原生 builtin/确认机制，不自动加载用户/项目扩展、Skills 或管理工具，不修改上游包。主侧共享工作目录，不承诺跨进程写入隔离或自动合并，停止不撤销已发生副作用。

### 回复时间

- 最终回复底部的复制、朗读、分支图标右侧显示本地时分，悬停或辅助技术可读取完整日期时间；生成中与中间过程回复不显示。
- 时间取原生消息的 timestamp，可能表示开始生成的时刻，不将其称为精确完成时间；无效或缺失时间不补造。

### 用户长消息折叠

- 主聊天中的用户正文超过 2,400 字符或 30 行时默认折叠，保留约 20 行预览，可展开全文并再次收起；普通消息完整显示。
- 复制仍取得完整问题，图片附件保持可见；当前聊天重绘保留展开状态，刷新页面后恢复默认折叠。支持中英文、键盘操作和手机布局。
- 展开入口采用无常驻边框的轻量文字与方向箭头，展开后箭头向上；悬停使用主题强调色，保留键盘焦点提示与手机 44px 点击高度。

### 执行记录来源

- 技能文件读取、扩展清单检查与包操作使用易读名称；扩展工具有调用时来源证据时显示来源标记，展开可核对原始工具与路径。
- 来源作为可选元数据随原生工具结果保存，历史重开保留；未知/旧记录不按当前安装状态补造归属，不把加载或读取当作模型已遵循技能。
- 包确认取消明确显示“已取消”。保留正文折叠、原始参数结果、错误和工具差异；支持中英文、流式与历史记录、手机布局。

### 默认能力与子 Agent 设置

- `npm ci` 默认尝试安装固定版本的 pi-subagents 0.69.0；可选能力下载失败不导致 Pivane 安装失败，保留尝试记录，不在启动或后续安装时隐式重试、覆盖已有版本或恢复用户已卸载的插件。
- “媒体模型”设置合并为“模型与能力”，包含“多模态”和“子 Agent”分区；未安装、停用或版本未适配时明确禁用子 Agent 配置，缺少插件时提供确认后的补装入口。
- 支持原生 settings.json 中的子 Agent 默认模型、思考等级与角色覆盖，保留其他插件字段；角色列表为随附角色与已配置覆盖，不冒充当前运行实例的完整角色发现。
- 保存保留运行中任务，提示空闲后重开运行实例；中英文与桌面、手机布局同步。

### 项目菜单与空闲提示

- 项目的导入 Pi 会话、项目信任和复制路径收进“更多操作”二级菜单，保留原有能力判断和操作范围。
- 线程菜单移除重复的待发送消息入口；输入区管理入口与侧栏队列提示保留。
- 恢复保留运行实例的绿色对勾“空闲”提示（沿用 RC3 样式）和 `/quit` 说明，没有运行实例时仍不显示“未运行”。仅调整展示，不改变运行实例的回收条件。

### 扩展助手弹窗

- 需求输入优先，示例改为轻量标签，范围下移，安装位置和项目路径默认折叠。
- 底栏突出“进入助手”主按钮，明确下一步在独立会话中确认发送；短屏幕正文内部滚动，保留操作入口。创建、草稿保留和安装确认语义不变。
- 中英文及桌面、手机布局同步调整，沿用主题颜色和手机16px输入字号。

### Agent 新建任务线程

- Agent 可在用户明确交办后创建同项目的正式线程并立即执行；支持本次模型/思考等级，省略时使用新线程默认配置，显式无效选择不静默回退。
- 任务先保存到原生会话，再启动唯一受管 worker；Agent 来源消息与用户发言区分，中英文回执支持进入目标线程及返回来源。
- 稳定任务标识防止重复创建和重放，启动失败仍保留原线程与任务；增加身份范围、并发/层级限制、状态查询及维护活动计数。
- 当前结果保留在目标线程，不含自动结果回传、任意已有线程互发消息或代码目录隔离。

### Packages / Skills 管理收简

- Packages 主视图保留包名称、范围、安装状态及常用操作；资源数量与逐项开关收进默认折叠的“高级设置”，手动链接安装按需展开。
- 资源优先显示现有 Skill 名称，缺失时回退文件或目录名，避免成排的 SKILL.md / index.ts；完整来源仍可展开核对。
- 包和技能的问号提供“了解”和“排查”，在独立助手中预填准确来源及范围。了解请求要求先只读说明功能、场景、示例和依赖，不自动发送或安装。
- 同步中英文、手机触摸与键盘菜单交互，保留原配置确认、信任、修订及旧后端兼容。

### 紧凑线程卡片

- 日常线程卡片移除固定的首条问题摘要，保留标题、更新时间和消息数；有标题时单行显示，无标题时首条问题最多两行，标题可悬停查看。保留留白、选中边框和主题配色。
- 搜索需要首条问题匹配上下文时才显示附近片段，清空搜索后收起；片段与标题仍按纯文本安全转义。临时会话的销毁说明和跨项目标签保留。
- 无运行实例的状态不再常驻；空闲实例、运行、未读、等待确认、失败、停止及未知等有效提示继续显示。仅改变前端展示，刷新生效，不改变归档、排序、未读判定或会话数据。
- 菜单忽略打开前已经发生、位置未变的延迟滚动事件，避免键盘打开后立即误关闭；实际滚动仍正常关闭菜单。

### 扩展助手

- 输入框加号新增“添加能力…”，设置 Packages / Skills 新增配置与排查入口，共用独立的 Pi 原生扩展助手会话；支持需求示例、安装范围、返回原会话与草稿附件保留。
- 助手按需求检查已有资源、检索来源与许可、配置依赖并验证；包操作通过专用工具复用原生配置互斥、修订和信任检查，网页确认后执行，不自动重载工作中的会话。
- 专用身份保存在原生 custom entry；普通会话不加载管理工具，媒体规划凭据不能调用包管理。打开入口不调用模型、不安装，迟到创建不抢回已取消或切换的页面。
- 中英文及桌面/手机入口同步提供。源码变更不表示已有部署已加载，也不承诺所有第三方技能、许可证或终端扩展都可适配。

## 1.0.0-rc.3 · 2026-09-13 · 会话整理与 Pi 更新

### 已有 Pi CLI 接入

- 普通安装复用当前用户的 Pi CLI 身份，新增只读目录解析命令，通过 Pi 公开 `getAgentDir()` 遵循 Windows/macOS/Linux 的系统主目录和环境覆盖规则，不复制认证或创建第二套模型配置。
- 三平台及英文安装步骤写入解析后的身份绝对路径，媒体/预约仍按实例保存；补充已有空身份切换、环境变量认证、WSL 差异、共享范围与外置身份备份说明。

### RC3 安装修订

本次以同一 RC3 版本重新发布，更新标签、归档和校验文件；精确源码提交与包 SHA256 见 Release 验收附件。此前已取得 RC3 的安装不会自动变更。

- 普通安装默认开放电脑目录：Linux/macOS 安装示例使用 `/`，Windows 示例使用可见盘符根；未配置或留空 `PI_PROJECT_ROOTS` 时后端也采用系统根（Windows 启动时发现存在的盘符）。显式配置继续生效，默认浏览位置仍优先主目录，系统文件权限保持。

- 明确普通安装与开发验收的边界、首次界面可用的交付标准，以及 Release 包和源码 checkout 的选择；后台常驻为可选后续步骤。
- macOS 安装示例在依赖安装前检查实际 Node22，命令链失败停止，补充版本管理器切换核对与只读健康检查。
- 补充项目浏览范围的安装选择和范围外目录排障，明确当前网页不能修改 `PI_PROJECT_ROOTS`，数据隔离不要求把项目限制在 Documents 或 demo 目录。

### 本版汇总

- 汇总 RC2 之后的会话标题、归档、系统提示词、辅助模型和设置改进；Pi Coding Agent 与配套官方依赖同步锁定为 0.85.1，应用与 lockfile 版本同步为 1.0.0-rc.3。精确验证范围见[RC3 说明](docs/releases/1.0.0-rc.3.md)及发布附件。

### 网页更新命令反馈

- 设置里的“更新 Pi”补齐实际命令、stdout/stderr、退出码和最终版本反馈，完成后自动刷新版本卡片；日志按行脱敏、去控制码并限制长度，以纯文本显示，不进入聊天记录。
- `node server.js` 自动接入与 `npm start` 相同的更新执行能力，已有服务无需更改启动命令；开发直连改为显式 `node server.js --direct`。更新执行支持 Node 22 和 24。
- 补齐RPC启动就绪检查：20秒内仅在同一进程重试只读get_state，拦截迟到探测回复；不重发用户消息或另开会话worker。
- 上游 `pi update` 不支持项目本地依赖，Pivane继续执行等价的本地精确版本更新，展示真实的npm命令与失败信息，不以全局Pi更新冒充工作台已更新。

### 辅助模型统一入口

- 使用偏好新增按用途列出的“辅助模型”，合并标题生成和媒体规划（原模块 Agent）选择，保留已有配置；齿轮展开用途说明和自动标题开关，支持统一保存与全部设为自动。
- 明确区分标题跟随当前线程、媒体采用规划默认规则的自动行为；显式选择的媒体规划模型不再失败后切换其他模型。主聊天模型、媒体执行确认和 Packages/Skills 管理保持各自入口。
- 统一用途登记、配置修订和批量原子保存复用原有偏好字段，旧入口变化可检测冲突。保存只验证，不执行任务；缺失统一能力的后端保留原界面。
- 增加辅助模型契约、后续用途接入要求、原生模型目录/私密保存与中英文桌面/手机验证。

### Pi 受管更新与维护

- 设置新增“更新 Pi”“仅备份”“重启实例”，采用服务端确认票据、执行前空闲复核与预约暂停；维护期间拒绝新工作，不重放失败或不确定的操作。
- `npm start` 改由独立启动器管理服务；Pi 官方配套依赖在新目录固定版本、安装并执行隔离 SDK/RPC/会话检查，再停机备份和切换。原安装保留，Pivane 应用更新继续使用发布包。
- 私有备份核对内核路径、身份、文件变化及 SHA-256，提供离线验证和原路径恢复；恢复前另存当前数据，未完成恢复阻止正常启动。私有运行目录从 Git、发布包和网页文件入口排除。
- RPC 清理等待实际进程退出，包括 SIGKILL 后的退出确认，正常停机补齐所有已登记的 RPC 进程；中英文确认、断连与进度界面覆盖桌面和手机。
- Linux ARM64/Node 22 隔离实例完成 Pi 0.85.0 → 0.85.1 的真实 npm 更新演练；其他平台的自动维护流程未做原生实机验收，未更新或重启现有个人实例。

### 专用标题模型

- 使用偏好的辅助模型入口支持独立选择标题生成供应商与文本模型，供全部线程自动命名和手动重新生成共用；默认跟随线程，与媒体规划及聊天模型互不影响。
- 保存时校验模型目录和认证，支持设置版本冲突保护；旧客户端仅更改开关时保留已选模型。指定模型移除或失败后保留原标题，不回退到昂贵的线程模型。
- 手动标题建议显示实际使用的模型和本次输入、输出、缓存 Token 用量。请求仍最多摘录 8,000 字符问答正文，不发送完整会话，不加入主聊天上下文，也不新建累计用量日志。
- 模型选择及其校验纳入既有配置/停机边界；覆盖独立模型的真实 RPC/本地模型验证、并发/失效保护和中英文桌面/手机界面回归。

### 使用偏好与 Packages 布局

- 使用偏好补齐语言选择、自动标题、模块 Agent 及展开通知控件的收缩/换行约束；触摸设备上的偏好面板限定纵向手势并保留缩放手势声明，避免横向拖动，正常纵向浏览继续可用。
- 原生 Packages 页增加配置资源概览、独立安装区、包状态卡片、资源来源折叠与空状态；中英文文案和手机布局同步，原有安装/更新/移除确认、资源开关、范围/信任/修订校验及完整搜索/40项分批保持。
- 新增只读刷新入口，读取失败后可重新核对，旧快照写按钮保持禁用；迟到读取不能覆盖较新范围。
- 浏览器回归覆盖两种语言、四种宽度与三个主题，包括新增偏好控件、展开通知、真实触摸滚动、包操作确认/冲突、读取失败恢复和范围竞争。前端刷新生效，无需重启；不表示 Safari 真机或其他部署实例已验收。

### 项目与线程归档

- 项目与持久线程支持独立归档/恢复，分别进入默认折叠的“已归档项目”和项目内“已归档线程”。可以直接打开查看，不必恢复；恢复项目保留线程各自的归档状态。
- 归档仅保存跨设备同步的工作台偏好，不删除目录、会话或媒体，不启动/停止worker。运行、未读、失败和等待确认的归档线程仍出现在工作中并带标记；最近会话排除归档。
- 列表与跨线程正文搜索提供“包含已归档”；持久修订保护迟到响应与搜索分页，保存失败不乐观隐藏或自动重放。保持原生会话、文件身份及权限校验。
- 会话树、搜索历史与书签、导出记录合并进“历史与记录”二级菜单，保留原功能、键盘和焦点行为；提供中英文界面。
- 新后端以`archives`标记启用；升级并重新加载后端后可用。接口与用户行为见[API](docs/API.md#项目与线程归档)和[用户指南](docs/USER_GUIDE.md#项目与线程归档)。

### 系统提示词

- 设置新增“系统提示词”，支持全局/项目追加指令、高级基础替换、Markdown 预览、修改差异和恢复默认/继承，直接管理 Pi 原生 SYSTEM.md/APPEND_SYSTEM.md 文件。
- 会话详情新增系统提示查看，按需读取当前唯一 worker 的正文与组成，显示来源推导、加载核对状态和时间，支持搜索、复制及跳转编辑。
- 保存保留当前任务，空闲时显式重新加载资源并核对；项目追加与全局追加不自动叠加。私有备份、文件修订冲突、跨分类草稿保留和迟到响应隔离随功能提供。
- 中英文界面及桌面/手机浏览器回归，新增原生 RPC 和 HTTP 隔离测试。`systemPrompts` 标记区分后端能力；源码完成不代表运行服务已经加载。

### 会话自动标题

- 新建未命名持久线程在首轮有效问答结束后后台命名一次，寒暄暂缓；已有历史、导入/复制和手动名称受保护。设置增加跨设备共享的自动命名开关，默认开启。
- 线程菜单新增“重新生成标题”：预览并编辑建议后显式保存。名称或上下文变化时拒绝迟到结果，关闭窗口保留原名称和主草稿。
- 独立模型请求仅使用有界问答正文，无工具/思考/图片或主聊天消息注入；生成状态和名称沿用 Pi 原生 metadata。额外用量不计入持久统计，失败/超时不自动重放，生成活动纳入回收/停机检查。
- 提供中英文界面、Node 原生 RPC/本地模型回归与桌面/手机浏览器用例。新后端以 `sessionTitles` 标记启用，代码完成不代表运行实例已经加载。

### 版本与更新

- 设置新增“版本与更新”，分别显示 Pivane、实际 Pi 和配套 Pi 版本，手动检查 GitHub/npm，支持正式版与包含预发布版渠道、SemVer 比较和 5 分钟进程内缓存。
- 提供官方发布说明、发布包与 SHA-256 下载及三平台手动更新步骤；上游 Pi 新版和 Pivane 配套版本分开展示。版本检查只读，安装与重启须通过受管维护确认。
- 查询固定来源、限制时间/正文大小，继承工作台身份和 Origin 校验；两个来源独立报告失败，错误不回传代理或远端正文。界面覆盖中英文和手机布局。

## 1.0.0-rc.2 · 2026-09-11 · 界面双语

- 当前界面加入简体中文/英文文案，首次按浏览器偏好选择，未匹配时回退英文；“设置 → 使用偏好”可保存当前浏览器的语言选择，下次打开或刷新页面生效，保存不自动刷新。
- 覆盖聊天控件、项目线程、历史、文件、侧聊、媒体清单、设置与常用应用提示；日期/数字按界面语言显示。聊天、文件、用户名称、已保存模板和模型/媒体参数保持原文；后台推送及部分外部诊断仍保留原语言。
- 增加英文项目介绍、安装与使用指南、语言契约及双语浏览器回归；平板设置分类改为网格，避免较长标签引起横向溢出。
- 版本及锁定清单同步为1.0.0-rc.2，独立生成新版归档。原RC1标签、归档和跨平台验收结果保持不变；本版精确验证范围随[发布说明](docs/releases/1.0.0-rc.2.md)和同名validation.json提供，不表示各部署实例已经更新。

## 主分支文档

- 完善项目首页的核心优势、手机/平板/电脑响应式体验、Linux/macOS/Windows部署入口和面向用户Agent的文档索引。
- RC标签与发布资产固定已验收源码；后续项目介绍更新不替换该归档。

## 1.0.0-rc.1 · 2026-09-11

- 首个公开候选版本；应用版本与包内Pi版本分别标识，发布说明见[RC说明](docs/releases/1.0.0-rc.1.md)。
- 补齐ISC许可证及第三方声明；新增版本化pack:release、跨平台串行npm test，以及绑定包SHA256的隔离安装/升级/恢复验收入口。
- 修复加载模型设置后SIGINT/SIGTERM可能提前结束异步清理的问题；停机监听保留至清理完成，重复信号不重复执行清理，失败仍以非零状态退出。
- Linux/macOS/Windows安装示例统一实际数据路径、归档校验、持久启动配置与再次启动步骤。

- 产品名称统一为 **Pivane**：网页、登录页、通知及候选包命名同步；Pi Agent仍使用上游Pi能力名称。
- 文档分为用户/用户Agent指南、公开开发契约与本地维护记录。公开文档清单、相对链接和私有目录排除加入构建检查。
- 改名沿用PI_*环境变量、pi5-*数据文件、API/RPC和浏览器偏好键；已有安装无需迁移目录或身份。

## 已完成的功能基线

- Pi原生持久/临时会话、模型登录与设置、流式工具、附件、Shell/停止/压缩、历史搜索/书签/树、分叉与导入导出。
- 文件差异和全文、临时侧聊、持久用量、回复朗读、主题与桌面/手机布局、页面通知及可选Web Push。
- 用户媒体服务与模型管理、常见HTTP输出/轮询协议、Agent接入草稿及确认单项执行；旧媒体历史与适配器兼容。
- Apple Silicon macOS与Windows11 x64原生文件句柄、路径、权限和持久化适配；Linux原有边界保持。Windows文件链接支持盘符、反斜杠与行号。
- 三个平台已完成相应安装、更新和同路径恢复基线，详细测试范围及未验收项见[平台说明](docs/RELEASE_INSTALL_VALIDATION.md)。基线通过不等于新候选版本已经公开发布。

用户安装与升级见[安装指南](docs/INSTALL_RECOVERY.md)。版本记录只描述可交付行为与兼容要求，维护者机器的部署日志不属于公开变更记录。
