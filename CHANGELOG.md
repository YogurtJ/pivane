# Pivane 变更记录

## 1.0.0-rc.3 · 2026-09-13 · 会话整理与 Pi 更新

- 汇总 RC2 之后的会话标题、归档、系统提示词、辅助模型和设置改进；Pi Coding Agent 与配套官方依赖同步锁定为 0.85.1，应用与 lockfile 版本同步为 1.0.0-rc.3。精确验证范围见[RC3 说明](docs/releases/1.0.0-rc.3.md)及发布附件。

### 网页更新命令反馈

- 设置里的“更新 Pi”补齐实际命令、stdout/stderr、退出码和最终版本反馈，完成后自动刷新版本卡片；日志按行脱敏、去控制码并限制长度，以纯文本显示，不进入聊天记录。
- `node server.js` 自动接入与 `npm start` 相同的更新执行能力，已有服务无需更改启动命令；开发直连改为显式 `node server.js --direct`。更新执行支持 Node 22 和 24。
- 补齐RPC启动就绪检查：20秒内仅在同一进程重试只读get_state，拦截迟到探测回复；不重发用户消息或另开会话worker。
- 上游 `pi update` 不支持项目本地依赖，Pivane继续执行等价的本地精确版本更新，展示真实的npm命令与失败信息，不以全局Pi更新冒充工作台已更新。

### 辅助模型统一入口

- 使用偏好新增按用途列出的“辅助模型”，合并标题生成和媒体规划（原模块 Agent）选择，保留已有配置；齿轮展开用途说明和自动标题开关，支持统一保存与全部设为自动。
- 明确区分标题跟随当前线程、媒体采用规划默认规则的自动行为；显式选择的媒体规划模型不再失败后切换其他模型。主聊天模型、媒体执行确认和 Packages/Skills 管理保持各自入口。
- 统一用途登记、配置修订和批量原子保存复用原有偏好字段，旧入口变化可检测冲突。保存只验证，不执行任务；缺失统一能力的后端保留原界面。
- 增加辅助模型契约、后续用途接入要求、原生模型目录/私密保存与中英文桌面/手机验证。

### Pi 受管更新与维护

- 设置新增“更新 Pi”“仅备份”“重启实例”，采用服务端确认票据、执行前空闲复核与预约暂停；维护期间拒绝新工作，不重放失败或不确定的操作。
- `npm start` 改由独立启动器管理服务；Pi 官方配套依赖在新目录固定版本、安装并执行隔离 SDK/RPC/会话检查，再停机备份和切换。原安装保留，Pivane 应用更新继续使用发布包。
- 私有备份核对内核路径、身份、文件变化及 SHA-256，提供离线验证和原路径恢复；恢复前另存当前数据，未完成恢复阻止正常启动。私有运行目录从 Git、发布包和网页文件入口排除。
- RPC 清理等待实际进程退出，包括 SIGKILL 后的退出确认，正常停机补齐所有已登记的 RPC 进程；中英文确认、断连与进度界面覆盖桌面和手机。
- Linux ARM64/Node 22 隔离实例完成 Pi 0.85.0 → 0.85.1 的真实 npm 更新演练；其他平台的自动维护流程未做原生实机验收，未更新或重启现有个人实例。

### 专用标题模型

- 使用偏好的辅助模型入口支持独立选择标题生成供应商与文本模型，供全部线程自动命名和手动重新生成共用；默认跟随线程，与媒体规划及聊天模型互不影响。
- 保存时校验模型目录和认证，支持设置版本冲突保护；旧客户端仅更改开关时保留已选模型。指定模型移除或失败后保留原标题，不回退到昂贵的线程模型。
- 手动标题建议显示实际使用的模型和本次输入、输出、缓存 Token 用量。请求仍最多摘录 8,000 字符问答正文，不发送完整会话，不加入主聊天上下文，也不新建累计用量日志。
- 模型选择及其校验纳入既有配置/停机边界；覆盖独立模型的真实 RPC/本地模型验证、并发/失效保护和中英文桌面/手机界面回归。

### 使用偏好与 Packages 布局

- 使用偏好补齐语言选择、自动标题、模块 Agent 及展开通知控件的收缩/换行约束；触摸设备上的偏好面板限定纵向手势并保留缩放手势声明，避免横向拖动，正常纵向浏览继续可用。
- 原生 Packages 页增加配置资源概览、独立安装区、包状态卡片、资源来源折叠与空状态；中英文文案和手机布局同步，原有安装/更新/移除确认、资源开关、范围/信任/修订校验及完整搜索/40项分批保持。
- 新增只读刷新入口，读取失败后可重新核对，旧快照写按钮保持禁用；迟到读取不能覆盖较新范围。
- 浏览器回归覆盖两种语言、四种宽度与三个主题，包括新增偏好控件、展开通知、真实触摸滚动、包操作确认/冲突、读取失败恢复和范围竞争。前端刷新生效，无需重启；不表示 Safari 真机或其他部署实例已验收。

### 项目与线程归档

- 项目与持久线程支持独立归档/恢复，分别进入默认折叠的“已归档项目”和项目内“已归档线程”。可以直接打开查看，不必恢复；恢复项目保留线程各自的归档状态。
- 归档仅保存跨设备同步的工作台偏好，不删除目录、会话或媒体，不启动/停止worker。运行、未读、失败和等待确认的归档线程仍出现在工作中并带标记；最近会话排除归档。
- 列表与跨线程正文搜索提供“包含已归档”；持久修订保护迟到响应与搜索分页，保存失败不乐观隐藏或自动重放。保持原生会话、文件身份及权限校验。
- 会话树、搜索历史与书签、导出记录合并进“历史与记录”二级菜单，保留原功能、键盘和焦点行为；提供中英文界面。
- 新后端以`archives`标记启用；升级并重新加载后端后可用。接口与用户行为见[API](docs/API.md#项目与线程归档)和[用户指南](docs/USER_GUIDE.md#项目与线程归档)。

### 系统提示词

- 设置新增“系统提示词”，支持全局/项目追加指令、高级基础替换、Markdown 预览、修改差异和恢复默认/继承，直接管理 Pi 原生 SYSTEM.md/APPEND_SYSTEM.md 文件。
- 会话详情新增系统提示查看，按需读取当前唯一 worker 的正文与组成，显示来源推导、加载核对状态和时间，支持搜索、复制及跳转编辑。
- 保存保留当前任务，空闲时显式重新加载资源并核对；项目追加与全局追加不自动叠加。私有备份、文件修订冲突、跨分类草稿保留和迟到响应隔离随功能提供。
- 中英文界面及桌面/手机浏览器回归，新增原生 RPC 和 HTTP 隔离测试。`systemPrompts` 标记区分后端能力；源码完成不代表运行服务已经加载。

### 会话自动标题

- 新建未命名持久线程在首轮有效问答结束后后台命名一次，寒暄暂缓；已有历史、导入/复制和手动名称受保护。设置增加跨设备共享的自动命名开关，默认开启。
- 线程菜单新增“重新生成标题”：预览并编辑建议后显式保存。名称或上下文变化时拒绝迟到结果，关闭窗口保留原名称和主草稿。
- 独立模型请求仅使用有界问答正文，无工具/思考/图片或主聊天消息注入；生成状态和名称沿用 Pi 原生 metadata。额外用量不计入持久统计，失败/超时不自动重放，生成活动纳入回收/停机检查。
- 提供中英文界面、Node 原生 RPC/本地模型回归与桌面/手机浏览器用例。新后端以 `sessionTitles` 标记启用，代码完成不代表运行实例已经加载。

### 版本与更新

- 设置新增“版本与更新”，分别显示 Pivane、实际 Pi 和配套 Pi 版本，手动检查 GitHub/npm，支持正式版与包含预发布版渠道、SemVer 比较和 5 分钟进程内缓存。
- 提供官方发布说明、发布包与 SHA-256 下载及三平台手动更新步骤；上游 Pi 新版和 Pivane 配套版本分开展示。版本检查只读，安装与重启须通过受管维护确认。
- 查询固定来源、限制时间/正文大小，继承工作台身份和 Origin 校验；两个来源独立报告失败，错误不回传代理或远端正文。界面覆盖中英文和手机布局。

## 1.0.0-rc.2 · 2026-09-11 · 界面双语

- 当前界面加入简体中文/英文文案，首次按浏览器偏好选择，未匹配时回退英文；“设置 → 使用偏好”可保存当前浏览器的语言选择，下次打开或刷新页面生效，保存不自动刷新。
- 覆盖聊天控件、项目线程、历史、文件、侧聊、媒体清单、设置与常用应用提示；日期/数字按界面语言显示。聊天、文件、用户名称、已保存模板和模型/媒体参数保持原文；后台推送及部分外部诊断仍保留原语言。
- 增加英文项目介绍、安装与使用指南、语言契约及双语浏览器回归；平板设置分类改为网格，避免较长标签引起横向溢出。
- 版本及锁定清单同步为1.0.0-rc.2，独立生成新版归档。原RC1标签、归档和跨平台验收结果保持不变；本版精确验证范围随[发布说明](docs/releases/1.0.0-rc.2.md)和同名validation.json提供，不表示各部署实例已经更新。

## 主分支文档

- 完善项目首页的核心优势、手机/平板/电脑响应式体验、Linux/macOS/Windows部署入口和面向用户Agent的文档索引。
- RC标签与发布资产固定已验收源码；后续项目介绍更新不替换该归档。

## 1.0.0-rc.1 · 2026-09-11

- 首个公开候选版本；应用版本与包内Pi版本分别标识，发布说明见[RC说明](docs/releases/1.0.0-rc.1.md)。
- 补齐ISC许可证及第三方声明；新增版本化pack:release、跨平台串行npm test，以及绑定包SHA256的隔离安装/升级/恢复验收入口。
- 修复加载模型设置后SIGINT/SIGTERM可能提前结束异步清理的问题；停机监听保留至清理完成，重复信号不重复执行清理，失败仍以非零状态退出。
- Linux/macOS/Windows安装示例统一实际数据路径、归档校验、持久启动配置与再次启动步骤。

- 产品名称统一为 **Pivane**：网页、登录页、通知及候选包命名同步；Pi Agent仍使用上游Pi能力名称。
- 文档分为用户/用户Agent指南、公开开发契约与本地维护记录。公开文档清单、相对链接和私有目录排除加入构建检查。
- 改名沿用PI_*环境变量、pi5-*数据文件、API/RPC和浏览器偏好键；已有安装无需迁移目录或身份。

## 已完成的功能基线

- Pi原生持久/临时会话、模型登录与设置、流式工具、附件、Shell/停止/压缩、历史搜索/书签/树、分叉与导入导出。
- 文件差异和全文、临时侧聊、持久用量、回复朗读、主题与桌面/手机布局、页面通知及可选Web Push。
- 用户媒体服务与模型管理、常见HTTP输出/轮询协议、Agent接入草稿及确认单项执行；旧媒体历史与适配器兼容。
- Apple Silicon macOS与Windows11 x64原生文件句柄、路径、权限和持久化适配；Linux原有边界保持。Windows文件链接支持盘符、反斜杠与行号。
- 三个平台已完成相应安装、更新和同路径恢复基线，详细测试范围及未验收项见[平台说明](docs/RELEASE_INSTALL_VALIDATION.md)。基线通过不等于新候选版本已经公开发布。

用户安装与升级见[安装指南](docs/INSTALL_RECOVERY.md)。版本记录只描述可交付行为与兼容要求，维护者机器的部署日志不属于公开变更记录。
