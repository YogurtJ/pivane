# Pi 依赖升级入口

此旧文件名保留为链接兼容入口。当前依赖与验证流程见 [依赖与上游适配](development/DEPENDENCIES.md)，运行中的 Pi 更新见 [版本与更新](UPDATES.md)。

具体版本以 `package.json`、锁定依赖和运行实例状态为准；本文件名中的 0.85.0 不代表当前使用的版本。历史 RC 的依赖与验收保留在相应 [版本说明](README.md) 中。
