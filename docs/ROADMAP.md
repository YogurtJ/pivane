# Pivane 路线图

## 当前已实现

- 原生Pi会话与唯一受管worker，浏览器流式回复、附件、工具、停止、压缩和运行恢复。
- 供应商API Key/OAuth、动态模型与Thinking、原生设置、Packages/Skills、项目信任与资源核对。
- 项目/线程管理、搜索、书签、会话树、分叉/重试/恢复、导入导出和一次性延迟消息。
- 临时侧聊（读取/检索与逐回复确认后的修改/命令；sideChatTools 标记）、文件差异/全文、数学公式/Mermaid、用量统计、回复朗读与通知入口。
- 媒体模型自配置、常见HTTP协议、文档接入草稿、参数规划与确认单项执行。
- Linux、已验收Apple Silicon macOS及Windows11 x64原生适配；具体范围见[平台验证](RELEASE_INSTALL_VALIDATION.md)。

## 首版发布准备

- [x] 产品名统一为Pivane，保留既有配置/协议/数据名称兼容。
- [x] 区分公开用户/Agent指南、公开开发契约和忽略的本地维护记录。
- [ ] 冻结正式候选版本，将最终包SHA256与相应测试绑定。
- [ ] 准备公开下载渠道、版本说明和反馈入口，再进行明确的对外发布。

## 后续候选方向

以下为计划，不表示当前已有能力，也没有固定完成日期：

- 更完整的真实供应商、手机/Safari和其他原生平台验收。
- 独立安装/后台服务入口、升级体验与恢复辅助。
- 更广的媒体协议、参考文件输入与持久批量工作流。
- 会话树完整备份/跨路径迁移、代码检查点与后台任务总览。

## 继续保持的边界

个人实例使用自己的身份和数据，不提供共享实例的多用户权限沙箱。Pi保持唯一会话事实来源；规划不授予执行权；失败或结果不确定不自动重放。扩展、工具、Shell和模型调用的权限不能因界面改动扩大。
