# Pivane 用户指南

简体中文 · [English](en/USER_GUIDE.md)

Pivane运行在你部署它的电脑或服务器上。浏览器可以在另一台设备上打开；项目目录、Shell和Agent工具使用的是服务器文件系统。

## 首次开始

1. 按[安装指南](INSTALL_RECOVERY.md)启动独立实例，打开启动日志中的网址。
2. 进入“设置 → 供应商与模型”，选择服务并完成API Key或OAuth登录。不要把Key发到聊天中。远程OAuth的localhost回调指服务器，按提供的设备码/手动回调步骤处理。
3. 关闭设置后选择服务器项目目录，新建线程，选模型与思考等级。已打开线程会在空闲时刷新模型目录；没有可用模型时使用页面上的设置引导。
4. 发送一个简单问题；刷新页面，核对持久线程和回复仍可打开。这个请求会使用你自己的供应商账户。

没有选择项目时，可以先管理全局Pi配置、Packages和Skills。`@`插入服务器项目文件路径；附件按钮、粘贴和拖放则读取浏览器提供的真实文件，两者用途不同。

## 自定义系统提示词

在 **设置 → 系统提示词** 选择所有项目或当前项目，通常使用“追加指令”补充回复偏好与工作习惯；需要替换默认基础行为时展开高级区域。编辑后可以查看 Markdown 预览和修改差异，再保存。恢复默认/继承也先形成草稿，保存后生效；原文件有私有备份。

项目提示词受项目信任控制，项目文件优先于全局文件；项目追加指令与全局追加指令不会自动叠加。保存不会打断正在进行的任务。空闲时点击“重新加载并核对”，或在会话详情 → 当前加载的资源 → 查看系统提示词，核对当前会话的正文、来源和读取时间。其他已打开会话需分别重载；刷新网页不保证更新运行实例。

草稿在当前页面内按项目/范围保留，关闭设置或切换分类后可以继续编辑；刷新页面前请保存。遇到冲突先“刷新并核对草稿”，检查最新磁盘内容与草稿的差异后再保存。详细范围、原生文件位置及限制见[Pi 原生配置](NATIVE_SETTINGS.md#系统提示词查看与编辑)。

## 界面语言

当前源码支持简体中文和英文。首次按浏览器偏好列表中首个受支持语言显示，中文变体使用简体中文，英文变体使用英文，没有匹配时回退英文。可在“设置 → 使用偏好 → 界面语言”选择“跟随浏览器 / 简体中文 / English”。选择只保存在当前浏览器的此站点，其他设备可独立设置。

保存后下次打开或刷新页面生效，页面不会自动刷新。刷新前请保留未发送草稿和附件；临时会话与侧聊会结束。界面语言不翻译聊天、文件、已保存模板或用户模型名称，也不修改模型请求和朗读语言。内置模板示例按页面语言提供，已保存的模板保持原文。

部分高级文档、上游与第三方诊断、后台推送文字仍保留原文；旧媒体界面不在首轮双语范围。已发布RC1归档不含此改动。详见[界面语言说明](I18N.md)。

## Packages 与资源

“设置 → Packages”先选择所有项目或当前项目。顶部概览显示扩展、Skills、提示词和主题的配置启用数与总数，不代表某个会话已经加载。安装区支持 npm、Git 和部署机器上的本地路径；安装、更新和移除仍需确认来源与范围。

已配置的包用卡片展示来源、范围和安装状态。下面的资源列表可搜索，默认每批显示 40 项；展开“查看来源”核对完整路径，选择启用/停用或项目继承后保存。未信任项目不允许项目写入。读取失败可点击刷新重新核对，不自动重试操作；配置保存后在会话空闲时重新加载资源。详见[Pi 原生配置](NATIVE_SETTINGS.md)。

手机上的“使用偏好”支持纵向触摸滚动，语言、自动标题、模块 Agent 和通知控件按可用宽度换行；页面的浏览器缩放入口保留。

## 版本与更新

“设置 → 版本与更新”分别显示 Pivane、实际 Pi 与发布包配套 Pi 的版本。点击“检查更新”查询 GitHub 和 npm，可选择正式版或包含预发布版，结果缓存 5 分钟；打开页面不会自动联网检查。可查看发布说明、下载发布包与校验文件，并展开对应平台的手动更新步骤。

工作台使用的 Pi 可以直接在此页更新：选择“更新 Pi”，确认后由服务器执行，实际命令、stdout/stderr、退出码及最终版本会显示在下方输出区。普通 `node server.js` 和 `npm start` 都自动支持，无需更改服务启动命令。更新会在新目录安装并验证精确版本，暂停预约、停机备份后切换；旧安装保留。全局 Pi CLI 和 Packages 扩展仍独立于工作台内核，也可单独选择“仅备份”或“重启实例”。

备份覆盖 Pi 身份、会话及媒体配置/记录/文件，不包含项目源码或外置链接目标；“仅备份”也会短暂停机。维护完成后核对版本、数据并逐项恢复预约。自动检查不代表所有第三方扩展已兼容，Pivane 应用本身仍按发布包手动更新。启用方法、失败处理及离线恢复见[版本与更新](UPDATES.md)。

## 对话与工具

正文视图默认折叠连续思考和工具记录，完整记录可逐项检查；浏览器会记住你的选择。运行中可以发送引导/后续消息，也可以停止。超时或断线时先核对线程状态，避免重复发送。

主输入框`!命令`执行服务器Shell，`!!命令`排除后续模型上下文但仍保留原生执行记录。工具使用当前系统用户权限，项目选择范围不构成沙箱。只安装你信任的Packages/扩展/Skills。

临时线程刷新或断开后结束，不保存会话文件。未发送草稿和附件只存在于当前页面，刷新或关闭前自行保留。

## 项目与线程归档

项目线程栏默认“全部”。项目菜单的“归档项目”把整个项目放到列表底部的“已归档项目”，线程菜单的“归档线程”把单条线程放到所属项目下的“已归档线程”；两处默认折叠，数量随实际列表显示。展开后可以直接打开查看，菜单中的“恢复项目 / 恢复线程”才将它移回常用区。查看不会自动恢复。

项目和线程归档互相独立：恢复项目不会恢复其中单独归档的线程；恢复线程也不会恢复它的项目。“随项目归档”表示该线程由项目归档收起。归档只保存展示偏好，不删除目录或会话，不停止正在运行的任务；状态跨设备同步，折叠展开只在当前页面记忆。

“工作中”仍显示归档线程的运行、新回复、失败和待确认提醒，并标注归档状态；它们不会进入最近会话。列表搜索和跨线程正文搜索默认不查归档项，勾选“包含已归档”后可查找旧内容。归档入口需要后端支持，旧后端不会显示不可用操作。

线程菜单的“历史与记录”二级菜单统一收纳会话树、搜索历史与书签、导出记录；复制名称和ID仍在“复制”二级菜单。方向键、返回和Escape沿用现有菜单操作。

## 辅助模型

“设置 → 使用偏好 → 辅助模型”按用途集中配置标题生成和媒体规划。每行可选择供应商与模型，齿轮展开说明及现有选项；修改后点击“保存更改”。已有标题模型和媒体“模块 Agent”选择会直接沿用，无需重新设置。

标题行的“自动”使用当前线程模型；媒体行的“自动”使用媒体规划默认规则，具体顺序在该行说明中。也可以为两项都选择同一个便宜模型。专用模型不可用或失败时不会自动换成其他模型。“全部设为自动”先修改选择，保存后生效，并保留自动标题开关。

这里仅管理已有的辅助任务，不改变主聊天模型、媒体执行模型或安装行为。Packages/Skills 的安装管理仍用原入口，尚无单独的 AI 搜索/审查辅助用途。完整范围与扩展方式见[辅助模型说明](AUXILIARY_MODELS.md)。

## 会话标题

“设置 → 使用偏好 → 辅助模型 → 标题生成”的齿轮中提供“自动生成会话标题”开关，默认开启，设置在本实例的设备之间共享。开启后新建且未填写名称的持久线程，会在首轮有效问答结束后后台生成一次短标题；寒暄会暂缓，之后的聊天不会持续改名。生成前仍显示首条问题的截断文本。已有历史、复制/导入的线程与手动名称不会自动覆盖。

在“标题生成”行可以保留“自动 · 当前线程模型”，或选择专用供应商和模型，再点击统一的“保存更改”。此选择与其他辅助用途和聊天默认模型独立，不改变任何线程的聊天模型。保存只核对模型目录与认证，不执行生成。只关闭自动命名开关也会保留模型选择，仍可手动生成。

线程菜单的“重新生成标题”使用上述模型设置生成建议，先在窗口里预览并编辑，点击保存才改名；关闭窗口保留原名称。如果生成期间别人改了名称或对话位置发生变化，旧建议不能覆盖新状态。原“重命名”仍可直接使用。生成成功后显示本次实际使用的模型，以及 Pi 返回的输入、输出和缓存 Token 数；缺失字段显示“—”，没有用量数据时明确提示未上报。

标题请求最多引用最近 6 条问题／完整结束的回答，每条最多 2,000 字符，合计最多 8,000 字符；即使会话很长，也不会发送完整历史。摘录不携带思考、工具输出或图片，也不在主聊天里增加消息。专用模型可与主线程属于不同供应商，这份摘录会发送给你选定的服务。它会产生额外模型用量，目前不计入持久会话用量统计，也不保存标题请求的累计用量日志。无法生成时保留原显示，不打断聊天、不自动重试；依赖扩展注册或请求钩子的特殊模型可能不能独立生成标题，可手动命名。关闭自动命名或更改标题模型不会取消已经发出的供应商请求；新选择用于后续请求，先前正在生成的自动结果会被丢弃。手动建议会标出它实际使用的模型，仍需明确保存。

## 历史、文件与侧聊

历史页可以搜索、保存书签和查看会话树。导航、编辑重试和恢复旧版本改变对话位置，**不会撤销文件、命令或外部请求**。复制/分叉创建新线程；HTML适合阅读，JSONL导出只含当前活动分支，完整备份仍需停机保存数据目录。

每轮成功的edit/write会形成文件列表。差异是原生操作记录；当时的写入内容和“当前文件”是不同来源，当前磁盘内容需要显式读取或刷新。长代码、公式与图表可在各自区域查看。

侧聊默认冻结主线程的有效背景，没有工具，可独立讨论再显式追加到主草稿。本页切换持久线程可保留有限段侧聊；刷新、关页和退出登录会结束它们。

## 多媒体实验室

新安装没有默认可执行的图像/视频/语音服务。在“设置 → 媒体模型”或实验室管理入口添加服务地址、Key和模型ID；按实际文档选择协议。聊天模型的凭据不代表媒体服务自动可用。

填写创作要求，让Agent生成方案或直接编辑常用参数；检查提交清单后确认单项生成。方案、连接探测和生成是不同操作。失败或结果不确定时先检查服务端任务和历史，不自动重试。回复旁的喇叭点击会按当前默认语音配置生成这一条朗读，可能收费。

## 管理与排障

用量统计读取持久会话中的原生记录，费用是历史配置估算，不等于供应商账单；临时会话、侧聊、标题生成和媒体规划不纳入该持久统计。配置保存通常对新运行实例生效，已有任务不会因此被自动停止。

本机通知可使用浏览器页面通知/提示音；手机后台推送需要HTTPS和系统支持。开启远程入口前检查访问验证。升级或备份前暂停预约并停止任务，再按[安装与恢复](INSTALL_RECOVERY.md)停机操作。

更多功能与接口见[文档目录](README.md)。让Agent排障时使用[操作指南](AGENT_GUIDE.md)，提供脱敏错误与实例路径，不提供凭据正文。
