# Pivane 1.1.0 · 2026-09-23

本版基于 **Pi Coding Agent 0.87.1**，推荐 **Node.js 22.x（至少 22.19）**。从 [GitHub Release](https://github.com/YogurtJ/pivane/releases/tag/v1.1.0) 下载安装归档与校验文件。1.0.0 的原归档保持不变。

## 本版变化

- **项目文件浏览**：右侧文件区提供目录树、文件名/相对路径搜索、隐藏文件开关、路径面包屑与定位。项目文件和本轮修改共用只读查看器；宽屏并排查看，手机切换目录与正文，浏览不会自动调用模型。
- **任务进度卡**：内置 `update_plan` 工具，展示 Agent 报告的步骤、状态与说明。计划保存在原生会话，随刷新、重开、分叉和历史导航恢复；停止或回复结束不会自行标记步骤完成。
- **模型选择与跨设备收藏**：模型面板支持目录搜索、常用和最近使用，按供应商/模型 ID 区分同名型号。常用由同一实例跨设备保存，最近使用仍在当前浏览器；旧收藏经确认回执合并，兼容局域网 HTTP。
- **手机布局与焦点**：手机顶部保留项目入口，状态条显示线程名称；模型、思考与上下文集中于底部面板。输入区上下文圆环和聚焦摘要减少常驻占用，模型选择器不再自动唤起手机键盘。文件、历史与阅读模式从详情页进入。
- **操作与设置反馈**：分叉、历史重试、预约、模板、资源重载和队列操作增加等待反馈与重复提交保护；较早响应不会覆盖较新的视图。重进模型设置时刷新本地目录，保留辅助模型草稿。
- **更新提醒**：前后版本对比、清晰的更新入口和可重复打开的预览；预览不安装、不改写提醒状态。导航折叠按钮同步细化。
- **Pi 0.87.1 兼容**：同步固定整套 Pi 依赖；导入与早期分叉保留原生上下文编辑、用量和无保留压缩记录。主聊天保留原文，模型和完整上下文侧聊遵循编辑投影；升级探针覆盖这些差异。

完整行为与限制见 [CHANGELOG](../../CHANGELOG.md)、[用户指南](../USER_GUIDE.md)、[模型设置](../PROVIDER_SETTINGS.md)、[文件浏览](../FILE_VIEWER.md)及[版本与更新](../UPDATES.md)。

## 从 1.0.0 升级

使用包含 Pivane 代码和 Pi 0.87.1 锁定依赖的完整发布包；只在旧应用中升级 Pi 不会加入本版兼容修复。支持应用受管更新的实例可从“设置 → 版本与更新”安装；其他实例按[安装与恢复](../INSTALL_RECOVERY.md)执行。

升级前保存草稿和临时对话、结束任务与 Shell、暂停预约，并停止使用同一身份的外部 CLI。停机整批备份后切换，保持原身份、媒体和会话路径。不会迁移目录或重写原生会话正文。恢复旧版时使用匹配的停机数据备份；旧 Pi 不理解新增上下文编辑语义，不能仅降级依赖后继续写新版会话。

收藏来自原设备浏览器时，须在原网址刷新并打开模型选择器一次，服务器才能接收旧收藏；其他设备随后读取共享结果。刷新网页加载新静态资源不会自动改变已有会话模型或全局默认值。

## 验收与发布资产

发布材料包含 `.tar.gz`、`.sha256`、逐文件清单、`validation.json`、版本说明及 `SHA256SUMS`。安装包固定已验收提交 `eb7ea8e5eda451a7146449ca45cc465a753c7f30`，SHA256 为：

```text
de39cd9f056aeb2dbc22122af9691083cbfaa9475fefe17b91ce2e03342d941b
```

505 个文件逐一匹配发行提交。发行标签和包内文档保留验收时的快照，主分支本页更新正式发布状态；不为改变文档中的候选称谓重新生成已验收安装包。

Linux ARM64 / Node 22.23.2 验收通过：292 项 Node 测试（无失败或跳过）、12 组 Chromium 浏览器专项、308 文件语法检查、68 文件公开文档检查及 0 生产依赖漏洞；独立安装、原生 RPC/Shell/历史/导入导出、从 1.0.0 升级和 17 文件原路径哈希恢复均通过。使用隔离身份与合成供应商，无付费模型请求。macOS、Windows、Linux x86_64、Safari、真实手机后台通知及所有真实模型/媒体供应商保留既有平台基线，不将浏览器模拟或旧包结果称为本包实机验收。

## English summary

Pivane 1.1.0 is pinned to Pi 0.87.1 and adds project file browsing, durable task progress, searchable model selection with shared favorites, and a more compact mobile workspace. It also improves asynchronous action feedback, settings catalog refresh and update notifications.

Upgrade the complete application and its locked dependencies together. Back up stopped data, preserve existing identity and media paths, and reload the browser after activation. Original transcripts remain intact while Pi context edits apply to model requests. Validation is bound to the exact archive. This stable release passed Linux ARM64 / Node 22 tests, upgrade from 1.0.0 and same-path restoration; other platforms retain their historical baselines.
